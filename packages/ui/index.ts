/**
 * Shared UI primitives.
 *
 * Currently empty. As the template evolves, components used across both
 * the web app and (potentially) other surfaces (email templates, marketing
 * sites) move here.
 *
 * For now, web-app-only components live in apps/web/components.
 */

export {};
